<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
</head>
<body>
  <h1>Welcome to the Tech Blog</h1>

  {{#if loggedIn}}
    <p>Hello, {{username}}! (User ID: {{userId}})</p>
  {{/if}}

  <ul>
    {{#each posts}}
      <li>
        <h2>{{this.title}}</h2>
        <p>{{this.content}}</p>
        <p>Posted by {{this.User.username}} on {{this.createdAt}}</p>

        <h3>Comments:</h3>
        <ul>
          {{#each this.Comments}}
            <li>
              <p>{{this.comment}}</p>
              <p>Comment by {{this.User.username}} on {{this.createdAt}}</p>
            </li>
          {{/each}}
        </ul>
      </li>
    {{/each}}
  </ul>
</body>
</html>